import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        margin: 15,
        flexDirection: 'row',
    },
    categoryContainer: {
        width: 80,
        alignItems: 'center',
    },
    image: {
        width: 70,
        height: 70,
    },
});
