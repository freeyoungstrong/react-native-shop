import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    productsList: {
        marginTop: 5,
        flexShrink: 10,
    },
    columnStyle: {
        justifyContent: 'space-around',
        margin: 5,
    },
});
